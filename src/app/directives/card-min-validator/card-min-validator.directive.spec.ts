import { CardMinValidatorDirective } from './card-min-validator.directive';

describe('CardMinValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CardMinValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
