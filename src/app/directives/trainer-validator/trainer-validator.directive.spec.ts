import { TrainerValidatorDirective } from './trainer-validator.directive';

describe('TrainerValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TrainerValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
