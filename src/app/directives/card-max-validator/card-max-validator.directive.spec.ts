import { CardMaxValidatorDirective } from './card-max-validator.directive';

describe('CardMaxValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CardMaxValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
