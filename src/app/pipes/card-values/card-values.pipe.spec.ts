import { CardValuesPipe } from './card-values.pipe';

describe('CardValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new CardValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
